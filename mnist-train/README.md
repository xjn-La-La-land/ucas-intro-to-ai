# 模式识别与机器学习实践作业

> 尝试使用不同的分类方法对 MNIST 手写数字数据集进行分类

* 逻辑回归方法（Logic Regression）
    ```bash
    python mlr.py
    ```

* 支持向量机方法（Support Vector Machine）: 线性SVM => 高斯核SVM
    ```bash
    python svm.py
    ```

* 决策树/集成学习方法: 决策树 => Bagging => AdaBoost (=> Random Forest)
    ```bash
    python dt.py
    ```